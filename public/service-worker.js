// Service worker placeholder for Smart O.R. PWA
self.addEventListener('install', (e) => {
    console.log('[Service Worker] Install');
});

self.addEventListener('fetch', (e) => {
    // console.log(e.request.url);
});
